import styled from "@emotion/styled";
import { Avatar, Box, Grid2, Typography } from "@mui/material";

export const ProfileBody = styled(Box)({
  position: "relative",
  padding: "10px 20px",
  height: "100%",
  backgroundColor: "#F0F0F0",
});
export const ProfileGrid = styled(Grid2)({
  height: "100%",
  width: "50%",
});
